<% include ../partials/header %>     

<div class="container">
    <div class="row">
        <div class="col-md-10 col-md-offset-1">
            <div class="thumbnail">
                <div class="caption-full">
                    <h1 class="text-center"><%= tutorial.title %></h1>
                    <h3 class="text-center"><%= tutorial.description %></h3>
                    <p class="text-center">
                        Created on <%= moment(tutorial.createdOn).format('MMMM Do YYYY, h:mm a') %> by <a href="/user/<% tutorial.author.username %>"><b><%= tutorial.author.username %></b></a>
                    </p>
                    <div id="content">
                        <%- tutorial.content %>
                    </div>
                    <% if (currentUser && tutorial.author.id.equals(currentUser._id)) { %>
                        <a href="/tutorials/<%= tutorial._id %>/edit" class="btn btn-warning"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit Tutorial</a>
                        <form action="/tutorials/<%= tutorial._id %>/?_method=DELETE" method="POST" id="delete-form">
                            <button class="btn btn-danger" id="tutorial-delete" data-toggle="modal" data-target="#confirm-delete" type="submit"><i class="fa fa-times" aria-hidden="true"></i> Delete Tutorial</button>
                        </form>          
                        <div id="confirm-delete" class="modal fade" aria-hidden="true">
                            <div class="modal-dialog">
                                <div class="modal-content">
                                    <div class="modal-header">
                                        <p>Confirm Delete</p>
                                    </div>
                                    <div class="modal-body">
                                        <p>Are you sure you want to delete this tutorial? This action <i><b>cannot</b></i> be undone.</p>
                                    </div>
                                    <div class="modal-footer">
                                        <button type="button" data-dismiss="modal" class="btn btn-danger" id="btn-ok">Delete</button>
                                        <button type="button" data-dismiss="modal" class="btn">Cancel</button>
                                    </div>
                                </div>
                            </div>
                        </div>
                    <% } %>
                </div>
            </div>
            <div class="well">
                <% if (tutorial.reviews.length === 0) { %>
                    <div class="text-center">
                        <h3>This Tutorial doesn't have any reviews...yet.</h3>
                        <h4><a href="/tutorials/<%= tutorial._id %>/reviews/new">Why not be the first?</a></h4>
                    </div>
                <% } else { %>
                    <div class="text-right">
                        <a class="btn btn-success" href="/tutorials/<%= tutorial._id %>/reviews/new">Add Review</a>
                    </div>
                    <% tutorial.reviews.forEach(function(review){ %>
                        <hr>
                        <div class="row">
                            <div class="col-md-8 col-md-offset-2">
                                <strong><a href="/user/<%= review.author.username %>"><%= review.author.username %></a></strong>
                                <% if (review.editedOn) { %>
                                    <span class="pull-right">Edited on <%= moment(review.editedOn).format('MMMM Do YYYY, h:mm a')%></span>
                                <% } else { %>
                                    <span class="pull-right">Posted on <%= moment(review.createdOn).format('MMMM Do YYYY, h:mm a')%></span>
                                <% } %>
                                <br />
                                <br />
                                <p><%= review.content %></p>
                                <% if (currentUser && review.author.id.equals(currentUser._id)) { %>
                                    <a href="/tutorials/<%= tutorial._id %>/reviews/<%= review._id %>/edit" class="btn btn-sm btn-warning pull-right"><i class="fa fa-pencil-square-o" aria-hidden="true"></i> Edit</a>
                                    <form action="/tutorials/<%= tutorial._id %>/reviews/<%= review._id %>?_method=DELETE" 
                                          method="POST" class="pull-right">
                                        <button type="submit" class="btn btn-sm btn-danger btn-delete"><i class="fa fa-times" aria-hidden="true"></i> Delete</button>
                                    </form>
                                <% } %>
                            </div>
                        </div>
                    <% }); %>
                <% } %>
            </div>
            
            <a class="btn btn-primary" href="/"><i class="fa fa-arrow-left" aria-hidden="true"></i> Go Back</a>
        </div>
    </div>
</div>

<% include ../partials/footer %>